"""Legacy harness package: drive the old texnitis_move_base_like C++ plugins
from Python so we can record `expected.json` fixtures for each scenario.

See README.md for the design rationale and tests/scenarios/SCHEMA.md for
the YAML format the runner consumes.
"""
