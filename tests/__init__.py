"""Top-level marker so tests/legacy_harness/ is importable as a package.

Required because tests/legacy_harness/run_legacy.py imports its sibling via
``from .legacy_runner import LegacyHarness``.
"""
